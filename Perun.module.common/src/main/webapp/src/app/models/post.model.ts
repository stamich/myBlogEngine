import DateTimeFormat = Intl.DateTimeFormat;

export class Post {

  postId: string;
  postTitle: string;
  postDate: DateTimeFormat;
  postContent: string;
  postAuthor: string;
  postCategory: string;
}
